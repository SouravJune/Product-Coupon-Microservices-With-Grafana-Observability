server:
  port: 8088

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/SouravJune/Product-Coupon-Microservices-With-Grafana-Observability
          clone-on-start: true